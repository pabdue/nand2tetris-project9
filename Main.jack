class Main {
  function void main() {
    var Array a;
    var int length;
    var int i, sum;
    let i = 0;
    let sum = 0;
    let length = Keyboard.readInt("How many numbers will you be adding? ");
    let a = Array.new(length);
    while(i < length) {
      let a[i] = Keyboard.readInt("Enter a number: ");
      let sum = sum + a[i];
      let i = i + 1;
      }
    do Output.printString("The sum is: ");
    do Out.printInt(sum);
    do Output.println();
    return;
    }
}